# Kino.pub API Configuration
# No credentials needed - using OAuth device flow with hard-coded client credentials
# API constants are built into the application
# Copy this file to .env and configure other settings as needed

# AI Provider Configuration (optional)
AI_PROVIDER=openai
OPENAI_API_KEY=your_openai_api_key_here

# Anthropic API Configuration
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Application Settings
MAX_RECOMMENDATIONS=10
MIN_RECOMMENDATION_SCORE=0.7
UPDATE_FREQUENCY=0 9 * * *

# Browser Settings
BROWSER_HEADLESS=true
BROWSER_TIMEOUT=30000